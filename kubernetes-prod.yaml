apiVersion: apps/v1
kind: Deployment
metadata:
    name: dragons-investments-ui
    namespace: fintech-prod-ersa-team
    labels:
        app: dragons-investments-ui
spec:
    replicas: 1
    selector:
        matchLabels:
            app: dragons-investments-ui
    template:
        metadata:
            labels:
                app: dragons-investments-ui
        spec:
            imagePullSecrets:
                - name: fintech-docker-secret
            containers:
                - name: dragons-investments-ui
                  image: ersa-team-docker-registry.fintechchallenge.pl/ersa-team/dragons-investments-ui:image_version_placeholder
                  imagePullPolicy: Always
                  env:
                      - name: API_BASE_PATH
                        value: https://dragons-investments-ui.ersa-team.prod.fintechchallenge.pl
                  ports:
                      - name: web
                        containerPort: 80
                        protocol: TCP
                  resources:
                      limits:
                          cpu: 5m
                          memory: 128Mi
                      requests:
                          cpu: 1m
                          memory: 64Mi
                  livenessProbe:
                      httpGet:
                          path: /
                          port: web
                      initialDelaySeconds: 10
                      periodSeconds: 10
                  readinessProbe:
                      httpGet:
                          path: /
                          port: web
                      initialDelaySeconds: 10
                      periodSeconds: 10
            restartPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
    name: dragons-investments-ui
    namespace: fintech-prod-ersa-team
    labels:
        app: dragons-investments-ui
        visualize: 'true'
spec:
    ports:
        - name: web
          port: 80
          protocol: TCP
          targetPort: web
    selector:
        app: dragons-investments-ui
---

---
# Note: in a kubernetes secret the string (e.g. generated by htpasswd) must be base64-encoded first.
# To create an encoded user:password pair, the following command can be used:
# htpasswd -nb user password | openssl base64

apiVersion: v1
kind: Secret
metadata:
    name: fintech-prod-ersa-team-basic-auth-secret
    namespace: fintech-prod-ersa-team

data:
    users: |
        ZXJzYTokYXByMSQwVWZFMEM2ZyQ4cHV4cnFXbkVTNjRhTy94SmVLVTAuCgo=

---
apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
    name: fintech-prod-ersa-team-basic-auth-middleware
    namespace: fintech-prod-ersa-team
spec:
    basicAuth:
        secret: fintech-prod-ersa-team-basic-auth-secret
---
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
    name: dragons-investments-ui
    namespace: fintech-prod-ersa-team
spec:
    entryPoints:
        - websecure
    routes:
        - kind: Rule
          match: Host(`dragons-investments-ui.ersa-team.fintechchallenge.pl`)
          middlewares:
              - name: fintech-prod-ersa-team-basic-auth-middleware
                namespace: fintech-prod-ersa-team
          services:
              - kind: Service
                name: dragons-investments-ui
                namespace: fintech-prod-ersa-team
                passHostHeader: true
                port: 80
                scheme: http
    tls:
        options:
            namespace: fintech-prod-ersa-team
        certResolver: hltech
        domains:
            - main: dragons-investments-ui.ersa-team.fintechchallenge.pl
